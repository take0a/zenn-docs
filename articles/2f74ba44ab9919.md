---
title: "Evidence入門"
emoji: "🏴"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ["evidence", "duckdb", "bi"]
published: false
publication_name: "robon"
---
# はじめに
# やってみた
## [インストール](https://docs.evidence.dev/install-evidence/)

VSCode拡張もありますが、やはり生ですよね。ということでコマンドラインからインストールしました。事前に nvm で node v24.15.0 を入れてあります。
```cmd
npx degit evidence-dev/template my-project
cd my-project
npm install
npm run sources
npm run dev
```

勝手にブラウザが立ち上がって、こんな感じになりました。
![](/images/2f74ba44ab9919/evidence.png)

## [新しいページを作る](https://docs.evidence.dev/build-your-first-app/#2-add-a-new-page)

見出しのリンクの本家のドキュメントのとおり、新しいページを作ります。
```md: pages/new-page.md
## Hello Evidence

This is a new page in Evidence.
```

## [マークダウンを書く](https://docs.evidence.dev/build-your-first-app/#3-write-markdown)
その次のページは、リンク切れで動きません。チケットが切ってありましたが、適当に直したものがこちらです。
```md: pages/new-page.md
## Hello Evidence

This is a new page in Evidence.

### This is a lower level header
This is some *italic* and **bold** text.

This is a [link](https://evidence.dev).

This is an image inserted using Markdown:
![alt text](https://docs.evidence.dev/img/wordmark-light.svg)

This is an image inserted using HTML:
<img src="https://docs.evidence.dev/img/wordmark-light.svg" alt="evidence logo" class="w-72"/>
```

## [データを使う](https://docs.evidence.dev/build-your-first-app/#working-with-data)
上の見出しに書いてあるとおり、`localhost:3000/settings/` につなぐとページの画面とは異なるこんな感じの画面が表示されます。
![](/images/2f74ba44ab9919/settings.png)

`Edit`を開けると、こんな感じ
![](/images/2f74ba44ab9919/edit.png)

で、これが、このファイルと対応してると言いたいのでしょう。
```yaml: sources/needful_things/connection.yaml
# This file was automatically generated
name: needful_things
type: duckdb
options:
  filename: needful_things.duckdb
```

## [クエリを書く](https://docs.evidence.dev/build-your-first-app/#5-set-up-a-source-query)
上の見出しに書いてある SQL は、既にあります。
```sql: sources/needful_things/order.sql
select * from orders
```

これが、`pages/index.md` 内で `needful_things.orders` として使えるよ。これを使えるようにする「おまじない」が `npm run sources` で、これをやると、本物のデータソースから Parquet 形式のキャッシュを作るというのが仕組みのようです。
データソースの SQL 方言で SQL ファイルにクエリを書いて、そのキャッシュに対するクエリは、マークダウンのコードブロック内に DuckDB 方言で書く。
````
```sql categories
  select
      category
  from needful_things.orders
  group by category
```
````
そのコードブロックの名前がデータセットとして、マークダウン内のコンポーネントタグ内で使用する。
```
<Dropdown data={categories} name=category value=category>
    <DropdownOption value="%" valueLabel="All Categories"/>
</Dropdown>
```
なので、Evidence 用に知る必要があるのは、[SQL](https://docs.evidence.dev/core-concepts/queries/) と [コンポーネント](https://docs.evidence.dev/components/all-components/) ということになります。

SQL については、DuckDB 方言とのことですが、データの受け渡しが SQL 間、コンポーネントとの間、ソースSQLなど独自の部分があるはずなので勉強しましょう。

コンポーネントは、結構ありますが、独自コンポーネントも作れるようなのでコダワル方への対応もできそうです。

# おわりに
ドキュメントの冒頭の Motivation が良いですね。

> We think it's still too difficult to build high quality data products. Businesses are stuck with BI software that delivers slow and clunky outputs, and analysts are stuck manually configuring reports.
>
> Our mission is to give you the tools to deliver production-quality data products that look and feel more like the New York Times' data journalism than a drag-and-drop dashboard.

Google 翻訳さんによる日本語訳。
> 高品質なデータ製品を構築するのは、依然として非常に困難だと私たちは考えています。企業は、動作が遅く使い勝手の悪いBIソフトウェアに縛られ、アナリストはレポートを手動で設定せざるを得ない状況に陥っています。
> 
> 私たちの使命は、ドラッグ＆ドロップ式のダッシュボードではなく、ニューヨーク・タイムズのデータ​​ジャーナリズムのような、洗練されたデザインと操作性を備えた、本番環境で使用できる高品質なデータ製品を提供するためのツールを提供することです。

加えて、AI の時代は、ノーコードじゃないと思うんですよね。逆に、全てをコードにせよ。Everything as Code だと思います。
人類が情報を伝え、まとめるために編み出した言語は最強。プログラムの時代にはプログラミング言語として、AI の時代にはコードとして。
